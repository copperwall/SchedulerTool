import java.awt.*;
import java.awt.event.*;
import javax.swing.*;
import javax.swing.UIManager;
import javax.swing.UnsupportedLookAndFeelException;
import javax.swing.table.*;
import javax.swing.border.*;
import java.io.File;
import java.net.URL;
import java.awt.image.BufferedImage;
import javax.imageio.ImageIO;

public class GenerateError {
   /* public static void main(String[] args) throws java.net.MalformedURLException {
      Icon gif = new ImageIcon("generate_error.png");
      JPanel wrapper = new JPanel();
      
      JLabel icon = new JLabel(gif);
      JLabel label = new JLabel("<html>ERROR: There are no courses in the database.</html>");
      
      icon.setVerticalAlignment(JLabel.CENTER);
      icon.setHorizontalAlignment(JLabel.CENTER);
      label.setHorizontalAlignment(JLabel.CENTER);
      label.setVerticalAlignment(JLabel.CENTER);

      wrapper.add(icon);
      wrapper.add(label);

      Box centerBox = new Box(BoxLayout.Y_AXIS);
      centerBox.setAlignmentX(JComponent.CENTER_ALIGNMENT);
      centerBox.add(Box.createVerticalGlue());
      centerBox.add(wrapper);
      centerBox.add(Box.createVerticalGlue());
      
      JFrame f = new JFrame("");
      f.setLayout(new BorderLayout());
      f.getContentPane().add(centerBox, BorderLayout.CENTER);
      f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
      f.pack();
      f.setVisible(true);
   } */
}  